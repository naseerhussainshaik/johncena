touch (file name)
vim (file name)
ls (list of foles)
rm (delete the file)
mkdir (directory)
rmdir 
cat
clear
cp (copy the file)
mv (move the file)
git clone url
git add .
git commit -m "add any commit or subject"
git push
git checkout -b master
git remote add origin url
git push origin master

